# histogram : a visual representation of distrubution of quantitive data.
# they group values into bins(intervals) and count how many falls in each range

import matplotlib.pyplot as plt
import numpy as np

